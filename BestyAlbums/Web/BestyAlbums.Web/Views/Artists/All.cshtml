@model IList<ArtistAllViewModel>

<div class="add-record-container text-center mx-auto">
    <a class="add-record-btn" href="/Artists/Add">Add Artist</a>
</div>
@if (@Model.Count > 0)
{
    <div class="container-standard">
        <table class="text-center mx-auto">
            @for (int i = 0; i < Model.Count; i++)
            {
                <tr>
                    <td class="record-card">
                        <img class="img-listing-size" src="@Model[i].ImageUrl" alt="Artist image" />
                        <p>Name: @Model[i].Name</p>
                        <p>Founded: @Model[i].Founded.ToString("yyyy-MM-dd")</p>
                        <p>Rating: @Model[i].Rating</p>
                        <p>Location: @Model[i].Location</p>
                        <a class="btn btn-primary rounded" href="/Artists/Edit/@Model[i].Id">Edit</a>
                        <a class="btn btn-danger rounded" href="/Artists/Delete/@Model[i].Id">Delete</a>
                    </td>
                </tr>
            }
        </table>
    </div>
}
else
{
    <div class="border container-standard text-center h1">
        <p>There are no artists yet.</p>
    </div>
}